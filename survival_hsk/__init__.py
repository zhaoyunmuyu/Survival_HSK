"""Compatibility package.

Docs and some scripts reference `survival_hsk.*` as the top-level module path.
The actual implementation currently lives under `survival_new_data.*`.

This package provides a thin wrapper layer so `python -m survival_hsk...` works.
"""

