# `survival_new_data` 代码结构

本目录用于承载基于 OpenRice 数据的预处理与建模代码，尽量与历史包解耦。

## 目录说明

- `survival_new_data/preprocess/`：数据预处理脚本（默认生成到 `<project-root>/survival_hsk/data/`）
- `survival_new_data/data/`：预处理产物说明文档（字段含义/来源等）

