"""Compatibility package.

Some modules in this repo historically used `survival.*` as the top-level import
path. The implementation now lives under `survival_st_gcn.*`.

This package provides a thin wrapper layer so older import paths keep working.
"""

